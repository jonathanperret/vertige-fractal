// Minimal service worker registration stub

export const register = (options?: {
  onUpdate?: (registration: ServiceWorkerRegistration) => void;
}): void => {
  // Service workers disabled in simplified app
};

export const unregister = (): void => {
  // Service workers disabled in simplified app
};
